import {
  widowFix
} from '../../../helpers/widow-fix';
import { module, test } from 'qunit';

module('WidowFixHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = widowFix(42);
  assert.ok(result);
});
