import {
  theContent
} from '../../../helpers/the-content';
import { module, test } from 'qunit';

module('TheContentHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = theContent(42);
  assert.ok(result);
});
